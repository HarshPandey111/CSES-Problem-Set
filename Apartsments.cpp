#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m,i,k,j;cin>>n>>m>>k;
    vector<int>a(n),b(m);   
    for(i=0;i<n;i++)cin>>a[i];
    sort(a.begin(),a.end());
    for(i=0;i<m;i++)cin>>b[i];
    sort(b.begin(),b.end());
    int c=0,i=0,j=b.size()-1;
    while(i<=j){
        
    }
    cout<<c;
    return 0;
}